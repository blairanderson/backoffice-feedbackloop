module.exports.baseUrl = function baseUrl(){
  var user = window.feedbackLoop;
  var teamUrl = "https://www.getfeedbackloop.com/api";
  if (user.devLocal) {
    teamUrl = "http://localhost:3000/api";
  }
  return teamUrl;
};
